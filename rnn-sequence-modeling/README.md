# RNN Sequence Modeling

Portfolio-style implementations of recurrent neural networks for:
- Text classification with BiRNN (GRU/LSTM) + self-attention
- Sequence labeling (POS tagging) with BiRNN

This module originated from MSc NLP coursework and is reorganized here in a clean, professional structure.
